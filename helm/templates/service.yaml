apiVersion: v1
kind: Service
metadata:
  name: {{ template "glue-tech-adapter.name" . }}
  labels:
{{- include "glue-tech-adapter.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 8888
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "glue-tech-adapter.name" . }}
